# General

theme = Homebrew

# Override dark (4) and light (12) blue to be cyan for better readability on dark backgrounds
palette = 4=#00a6b2
palette = 12=#00e5e5

# Unfocused split: do not add overlay
unfocused-split-opacity = 1
# Homebrew dirty yellow
split-divider-color = #999900

font-size = 10
mouse-hide-while-typing = true
confirm-close-surface = false

# Copy on text select to the system clipboard
copy-on-select = clipboard

# The `no-cursor` forces the cursor to be a block
shell-integration-features = no-cursor,sudo,title,ssh-env,ssh-terminfo

# Macos specific

macos-titlebar-style = tabs

keybind = super+shift+r=reload_config

# ------ Emacs-like keybindings ------

keybind = ctrl+x>2=new_split:down
keybind = ctrl+x>3=new_split:right
keybind = ctrl+x>k=close_surface

keybind = ctrl+x>5>2=new_window

keybind = super+left=goto_split:previous
keybind = super+right=goto_split:next
keybind = ctrl+x>p=goto_split:previous
keybind = ctrl+x>o=goto_split:next

keybind = ctrl+x>b=new_tab
keybind = ctrl+super+left=previous_tab
keybind = ctrl+super+right=next_tab
# keybind = ctrl+x>t=prompt_tab_title

keybind = super+shift+,=scroll_to_top
keybind = super+shift+.=scroll_to_bottom
keybind = ctrl+up=scroll_page_fractional:-0.3
keybind = ctrl+down=scroll_page_fractional:0.3
